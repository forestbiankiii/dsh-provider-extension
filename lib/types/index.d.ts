/** Host loader entry for the model-panel browser implementation exported from `./client`. */
/** Host plugin body — this package owns browser behavior only. */
export declare function apply(): void;
