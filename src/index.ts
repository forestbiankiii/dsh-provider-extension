/** Host loader entry for the provider-extension browser implementation exported from `./client`. */

/** Host plugin body — this package owns browser behavior only. */
export function apply(): void {}
